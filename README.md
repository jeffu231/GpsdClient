# GpsdClient
A .NET 8 library for connecting in an easy way, to different type of gps devices inside C#.

The library contains the following ways to connect to gps services:
- Using the GPSD service (not officialy supported by GPSD).


## Example
Included is a simple console application that shows how to use the library.

